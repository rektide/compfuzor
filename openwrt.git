---
- hosts: all
  tags:
  - source
  gather_facts: False
  vars:
    TYPE: openwrt
    INSTANCE: git
  vars_files:
  - vars/common.vars
  - vars/src.vars
  tasks:
  - include: tasks/cfvar_includes.tasks
  - git: repo=https://github.com/mirrors/openwrt.git dest=${DIR.stdout}

