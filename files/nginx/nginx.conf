{% for outter in outter_scope.keys() -%}
{{ outter }} {{ outter_scope[outter] }};
{% endfor -%}

events {
{% for event in events.keys() -%}
{{ "\t" }}{{ event }} {{ events[event] }};
{% endfor -%}
}

http {
	include {{ ETC.stdout }}/conf.d/*conf
}
