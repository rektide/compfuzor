# Fixed-size swap, placed last (filename sort = on-disk order) so root can
# grow into everything before it. @SWAP@ is stamped at run time via stamp.sh
# REPART_SED (e.g. REPART_SED='s|@SWAP@|4G|g').
[Partition]
Type=swap
Format=swap
Label=swap
SizeMinBytes=@SWAP@
SizeMaxBytes=@SWAP@
