#!/bin/sh
set -e
[ -b "$SYSLINUX_DEV_FAT" -a -b "$SYSLINUX_DEV_LINUX" ] || exit 1
[ -d "$SYSLINUX_MNT_FAT" -a -d "$SYSLINUX_MNT_LINUX" ] || exit 2
mount "$SYSLINUX_DEV_FAT" "$SYSLINUX_MNT_FAT"
mount "$SYSLINUX_DEV_LINUX" "$SYSLINUX_MNT_LINUX"
