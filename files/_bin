#!/bin/bash
{% set _item_helpers = item.helpers | default(none) %}
{% set _nuclear = _item_helpers is sameas false %}
{% set _fallback_type = none if item.generated_by | default('') == 'gen_bins' else TYPE | default(none) %}
{% set _disarm = item.name | resolve_bin_disarm(
  item.origin_subsystems | default(none),
  item.bypass_scopes | default(none),
  item.bypass | default(none),
  _fallback_type,
) %}
{% set _active_bypass = _disarm.entries | length > 0 and not _nuclear %}
{% set _cf_verb = item.verb | default(_disarm.verb) %}
{% set _helper_layers = [
  DEFAULT_HELPERS | default(['env', 'setopts', 'loud']),
  item.base_helpers | default(none),
  ['report', 'guard'] if _active_bypass else none,
  _item_helpers,
] %}
{% set _helpers = [] if _nuclear else _helper_layers | resolve_helpers %}
{% for _h in _helpers %}
{{ _h | helper_comment }}
{% include "_helpers/" ~ _h %}

{% endfor %}
{% if not _nuclear %}

{% if item.comments is defined %}#{{item.comments}}{% endif %}

{% for var in item.vars|default([]) %}
export {{var}}="{{ '${' }}{{ var }}-{{ lookup('vars', var) }}{{ '}'}}"
{% endfor %}
{% if 'env' in _helpers %}
{% if item.basedir|default(True) == True %}
cd "${DIR}"
{% elif item.basedir|default(True) == False %}
{% elif item.basedir[0] == "/" or item.basedir[0] == "~" %}
cd {{item.basedir}}
{% else %}
cd {{DIR}}/{{item.basedir}}
{% endif %}
{% endif %}

{% endif %}
{% if _active_bypass %}
_cf_action_init "{{ _disarm.name }}" "{{ _cf_verb }}" "{{ _disarm.report_labels }}"
{% for _cf_entry in _disarm.entries %}
{% if ':' in _cf_entry %}
{% set _cf_parts = _cf_entry.split(':', 1) %}
if ! reason="$(_cf_run_guard 3 _cf_guard_bypass_unit "{{ _cf_parts[0] }}" "{{ _cf_parts[1] }}" "{{ _cf_verb }}")"; then _cf_report_skip "{{ _disarm.name }}" "$reason" "{{ _disarm.report_labels }}"; _cf_action_end; exit 0; fi
{% else %}
if ! reason="$(_cf_run_guard 3 _cf_guard_bypass "{{ _cf_entry }}" "{{ _cf_verb }}")"; then _cf_report_skip "{{ _disarm.name }}" "$reason" "{{ _disarm.report_labels }}"; _cf_action_end; exit 0; fi
{% endif %}
{% endfor %}
{% endif %}
{{ item.early|join2("\n") }}
{{ item.generated|join2("\n") if item.generatedAt|def == "early" else omit }}
{{ item.content|join2("\n") }}
{{ item.exec|join2("\n") }}
{{ item.execs|join2("\n") }}
{{ item.generated|join2("\n") if item.generatedAt|def("end") == "end" else omit }}
{% set _cf_run_all = item.run_all|normalize(to='list') %}
{% if _cf_run_all|length > 0 %}
{% for run_bin in _cf_run_all %}
if [ "$_cf_loud" = 1 ]; then printf '+ %s\n' '{{ run_bin }}'; fi
"$DIR/bin/{{ run_bin }}" "$@"
{% endfor %}
if [ "$_cf_loud" = 1 ]; then printf 'complete\n'; fi
{% endif %}
{{ item.late|join2("\n") }}
{% if _active_bypass %}
_cf_action_end
{% endif %}
{% for _h in _helpers | reverse %}
{% set _epi = "_helpers/" ~ _h ~ ".footer" %}
{% include _epi ignore missing %}
{% endfor %}
