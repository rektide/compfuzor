#!/bin/bash
set -e
{% if ENV|default(False) or has_env.stdout|int == 1 -%}
source "{{DIR}}/env.export"
{% endif %}
cd {{DIR}}{{ "/"+item.basedir if item.basedir is string else "" }}

{{ item.exec|default("") }}
{% for line in item.execs|default([]) %}
{{ line }}
{% endfor %}
