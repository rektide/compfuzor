- name: "get urls"
  get_url:
    url: "{{_url}}"
    dest: "{{_dest}}"
    owner: "{{OWNER|default(omit)}}"
    group: "{{GROUP|default(omit)}}"
    validate_certs: "{{VALIDATE_CERTS|default(true)}}"
  with_items: "{{GET_URLS|default([])|arrayitize}}"
  become: "{{ COMFUZOR_SHOULD_BECOME|default(BECOME|default(_dest|dirname|should_become(OWNER|default(), _cf_user_id, GROUP|default(), _cf_user_gid)))|bool }}"
  when: GET_URLS|default(False) is iterable
  vars:
    _url: "{{item.url if item is mapping else item}}"
    _dest: "{{(item|basename if item is string else item.dest)|defaultDir(SRC)}}"

- name: "save '.url' file"
  copy:
    content: "{{_url}}"
    dest: "{{_dest}}.url"
    owner: "{{OWNER|default(omit)}}"
    group: "{{GROUP|default(omit)}}"
  with_items: "{{GET_URLS|default([])|arrayitize}}"
  become: "{{ COMFUZOR_SHOULD_BECOME|default(BECOME|default(_dest|dirname|should_become(OWNER|default(), _cf_user_id, GROUP|default(), _cf_user_gid)))|bool }}"
  when: GET_URLS|default(False) is iterable
  vars:
    _url: "{{item.url if item is mapping else item}}"
    _dest: "{{(item|basename if item is string else item.dest)|defaultDir(SRC)}}"
