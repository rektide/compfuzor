---
- name: "Compfuzor: synthesize python subsystem artifacts"
  set_fact:
    BINS: "{{ BINS | merge_list_subsys('python') }}"
    ENV: "{{ ENV | merge_dict_subsys('python') }}"
    ETC_FILES: "{{ ETC_FILES | merge_list_subsys('python', path='contrib.ETC_FILES', strategy='append') }}"
  when: lookup('subsys', id='python', get='active', default=false) | bool
