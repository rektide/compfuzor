---
- name: "Compfuzor: create directory for cvs import"
  file: path="{{REPO_DIR|def(DIR, falsy=True)}}" state=directory
- name: "Fetch cvs repository as git with cvsimport"
  shell: chdir="{{REPO_DIR|def(DIR, falsy=True)}}" git cvsimport -d "{{CVS_REPO}}" -C . -r cvs -k "{{CVS_MODULE}}"
