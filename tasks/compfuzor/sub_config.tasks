---
- name: "Compfuzor: compile config declarations"
  set_fact:
    SUBSYSTEM: "{{ SUBSYSTEM | config_publish(compiled) }}"
  vars:
    compiled: >-
      {{ CONFIGS | default({}) | resolve |
         config_compile(ETC, BINS_DIR, CONFIG_DISABLED_SUFFIX) }}
  when: CONFIGS is defined
