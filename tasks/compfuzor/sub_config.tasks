---
- name: "Compfuzor: compile drop-in and config declarations"
  set_fact:
    _config_plan: >-
      {{ DROPINS | default({}) | resolve |
         config_compile(CONFIGS | default({}) | resolve) }}
  when: DROPINS is defined or CONFIGS is defined
