- name: "Compfuzor: generate password variables"
  command: pwgen 11 -n1
  with_items: "{{ PASSWORD|arrayitize }}"
  register: _passwords
- name: store passwords
  set_fact:
    "{{item.item}}": "{{item.stdout}}"
  with_items: "{{_passwords.results}}"
